# Bind to Azure port in App Service
server.port=${PORT:8080}

# Move actuator to /cil/actuator
management.endpoints.web.base-path=/cil/actuator

# Expose only the health endpoint
management.endpoints.web.exposure.include=health

# IMPORTANT: Keep details hidden so response is exactly {"status":"UP"}
management.endpoint.health.show-details=never
